# AGENTS.md

Limit number of comments in code and make them concise.

## Updating readme

`readme.md` is the specification: it states what the extension does, as plain
behavioral rules — not features, marketing, or implementation.

When you change behavior, update `readme.md` in the same change and keep it minimal.
